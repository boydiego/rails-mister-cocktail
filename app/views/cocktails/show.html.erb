<%= @cocktail.name %>
<br>
<ul style="list-style: none; padding: 0">
  <% @cocktail.doses.each do |dose| %>
    <li> <%= dose.ingredient.name %> <%= dose.description %> <%= link_to "🚮delete", dose_path(dose), method: :delete, data: { confirm: "Remove ingredient?"} %>
    </li>
  <% end %>
</ul>

<br>
<%= link_to "➕ add ingredients", new_cocktail_dose_path(@cocktail) %>
<br>
<%= link_to "👁‍🗨 cocktails list", cocktails_path %>
